export const metadata = {
  title: "Cleaning AI Assistant",
  description: "Dashboard for job scheduling",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
